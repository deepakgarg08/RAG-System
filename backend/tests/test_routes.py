"""
test_routes.py — Integration tests for FastAPI HTTP endpoints.
Tests POST /api/ingest, POST /api/query (SSE), and GET /health.
Uses httpx AsyncClient against the test app; all backend services are mocked.
"""

# TODO: implemented in Prompt 1B
