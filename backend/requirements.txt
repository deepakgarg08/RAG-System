# Web framework
fastapi==0.111.0
uvicorn==0.30.1

# AI / LLM
# Note: openai 2.x has minor API changes vs 1.x — client init is the same
openai==2.30.0
langchain==1.2.13
langchain-openai==1.1.12
langchain-community==0.4.1
langchain-text-splitters==1.1.1
langgraph==1.1.3

# Vector store (DEMO)
# Note: chromadb 0.5.x uses Pydantic v1 which is incompatible with Python 3.12+
# Use 1.x which uses native Pydantic v2
chromadb==1.5.5

# Document processing
# Note: pin >=, not exact, as older versions don't build on Python 3.12+
pymupdf==1.27.2.2
Pillow==12.1.1
pytesseract==0.3.13
langdetect==1.0.9

# API utilities
python-dotenv==1.2.2
python-multipart==0.0.9
pydantic==2.12.5
pydantic-settings==2.13.1
httpx==0.27.0

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# Azure SDKs (PRODUCTION — install when ready to migrate)
# azure-search-documents==11.6.0b4
# azure-storage-blob==12.20.0
# azure-ai-formrecognizer==3.3.3
